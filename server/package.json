
{
  "name": "home-value-ai-server",
  "version": "1.0.0",
  "description": "Backend server for HomeValueAI property price prediction",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "train": "cd ../model_training && python train_model.py",
    "setup-python": "cd ../model_training && pip install -r requirements.txt"
  },
  "dependencies": {
    "@tensorflow/tfjs-node": "^4.17.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
